{
  "name": "electron",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "electron .",
    "dev": "nodemon --watch . --exec electron .",
    "pack": "electron-packager . ElectornPackage --win --out ./ElectornPackage --arch=x64 --app-version=1.0.0 --electron-version=21.1.0 --overwrite  --icon=./src/assets/favicon.ico --ignore=node_modules"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^21.1.0"
  }
}
